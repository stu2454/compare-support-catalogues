version: '3.8'

services:
  streamlit-app:
    build: .
    container_name: ndis-catalogue-comparison
    ports:
      - "8501:8501"
    volumes:
      - ./assets:/app/assets
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    restart: unless-stopped
